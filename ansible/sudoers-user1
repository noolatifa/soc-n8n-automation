# /etc/sudoers.d/user1 — mode 0440, owner root:root
#
# IMPORTANT: a 644 file is REJECTED by sudo and makes every become task
# hang until timeout. Install with:
#   sudo install -m 440 -o root -g root sudoers-user1 /etc/sudoers.d/user1

Defaults:user1 !authenticate
user1 ALL=(ALL) NOPASSWD: ALL